/**
 * Attiva il contatore di caratteri al campo contenente la descrizione della recensione
 */
function addInputHandlers() {
    /* Inizializza il contatore del campo per la descrizione */
    initCounter(document.getElementById("registra-recensione"), document.getElementById("feedback"));
}